Jac: Py superset AI+graph. pip install jaclang. jac run/serve/test file.jac. with entry{code}. Types required. .jac .impl.jac .test.jac.
Types: int str float bool any list[T] dict[K,V] set[T] tuple[T,T] T|T T|None. enum E{V1,V2}. if/elif/else while for. match v{case t():code;case _:x}. try{}except T{}. lambda p:type:expr.
def n(p:t=v)->r{x}. async def. @dec. obj N{has a:t;def m(){}}. N(a=v). obj C(P){}. impl N.m{x}. def postinit{}.
node N{has a:t;def m;can ab with W entry|exit{}}. here visitor self. ++> <++>. +>:E:+> <+:E:<+. [n-->(`?T)](?a==v).
edge E{has p:t;can ab}. ++> <++>. +>:E(p=v):+>. [n->:E:->] [n->:E:p>v:->]. del edge.
walker W{has s:t;can ab}. n spawn W() root spawn W(). visit [-->][<--][<-->]. visit [-->(`?T)]. visit [->:E:->]. disengage skip. report v. can ab with entry|exit. can ab with `root entry{}.
[n-->] [<--n]. [-->(`?T)] [-->](?a==v). [->:E:->] [-->-->]. [-->(`?T)](?a>5)-->(`?G)].
root per-user. save(o) commit(). MongoDB.
pip install byllm. import from byllm{Model,Image}. llm=Model(model_name="gpt-4o"). def f(p)->r by llm(). Image("p"). by llm(method='Reason|ReAct',tools=[],stream=T). temperature max_tokens.
jac serve→:8000/docs. /walker/{n} /walker/{n}/{id}. obj __specs__{static has methods:list=["get","post"];auth:bool=T;as_query:list|"*";private:bool;path:str}. {"status":200,"reports":[],"returns":[]}. methods:["websocket"]. webhook:dict. schedule:dict={"trigger":"cron|interval|date","hour":"0"}. has file:UploadFile. async walker→id; wlk=&id;wlk.__jac__.status.
DATABASE_HOST DATABASE_NAME REDIS_HOST TOKEN_SECRET TOKEN_TIM   EOUT UV_HOST UV_PORT LOGGER_LEVEL SHOW_ENDPOINT_RETURNS SSO_{P}_CLIENT_ID SSO_{P}_CLIENT_SECRET.
NoPerm ReadPerm ConnectPerm WritePerm. _.allow_root(n,t,l) grant(n,l). _.disallow_root(n,t) revoke(n). _.check_read_access(n). def __jac_access__()->L{}.
APPLE GOOGLE GOOGLE_ANDROID GOOGLE_IOS FACEBOOK GITHUB. /sso/{p}/register/callback?id_token={t}.
test n{assert x;assert v==e}. jac test f.jac.
Compute→data. n=loc e=rel w=agents ab=events. loc entry→w entry→visits→w exit→loc exit. FIFO queue.
BFS w{visited queue}. DFS w{visited recursive}. RAG node Doc{text embedding}. embed find_similar answer by llm(). Social n User;e Follow;n Post. w GetFeed. Ecom n Product Order;e Contains. w Checkout. Task n Task;e DependsOn. w Schedule. Game def gen_level by llm(). Trade def transaction def npc_chat by llm(method='ReAct'). Multi enum QType. def classify by llm(). w Route.
Singleton Pagination. n Cache{key val expires}. n RateLimit{user cnt window}. n AuditLog{act user ts}. is_deleted deleted_at. n Version. n Event. per-root. async w+sched.
Actor CQRS Saga Pub-sub.
Filter early [-->(`?T)](?a>v). Batch commit(). Gen. Cache. Async. Limit depth.
print() printgraph(n,d). w Debug. curl. /docs. tail -f. printgraph(dot_file="g.dot");dot -Tpng g.dot.
Py libs REST DBs queues.
class→obj/node. __init__→has. collections→n/e. iter→w.
v0.8+: impl not :obj:. C(B) not :B:. def/can. visitor not here in n. lambda x:t:e. ->:E:-> +>:E:+>. import from p{i}. perm_grant/revoke. assert. global nonlocal. printgraph. Model(model_name).
n=entities obj=values w=ops. .jac/.impl/.test. Type. Validate. Try/except. Report. Limit depth. Index root. Batch. Cache. Async. Auth. Perm. Env. HTTPS. Log. Monitor. Doc. Sem.
with entry has can def impl glob obj node edge walker spawn visit disengage skip report sem by `root self here visitor path del async/await match/case try/except/raise import/from for/while if/elif/else return break/continue and/or/not in is lambda type enum.
jac run/serve/test/build/format/check/clean. jac tool ir ast/py. --verbose --debug.
jac-streamlit. jac streamlit. jaclang-extension. Jac-Lens. Jac Playground.
Scale-agnostic. Object-Spatial. AI-first. Graph-native. Py superset. Type-safe. Auto-API. Per-user root. Event-driven. Bidirectional.
has n:t=v. glob n=v. def n(p)->t{x}. obj N{has;def}. node N{has;def;can}. edge N{has}. walker N{has;can}. n++>n. [n-->]. [e](?c). spawn w(). visit [n]. report v. import from p{i}. with entry{x}. test n{assert}. def f()->t by llm(). lambda x:t:e. match v{case:x}. try{}except{}. impl C.m(){}. `root. here visitor self path. disengage skip. del e. save(o) commit().
