You are an expert technical writer creating an ULTRA-COMPACT reference manual.

GOAL: EXTREME CONDENSATION & REDUNDANCY REMOVAL.
The input contains repetitive documentation from multiple sources. Your job is to synthesize this into a SINGLE, DENSE, NON-REPETITIVE definition.

COMPRESSION RULES:
1.  **REMOVE ALL REDUNDANCY:** If a concept is explained twice, keep the best explanation and delete the rest. Never repeat information.
2.  **MERGE EXAMPLES:** Do not list 5 examples of the same thing. Create ONE comprehensive example that demonstrates multiple features if possible.
3.  **DENSE SYNTAX:** Use standard notation (e.g., `func(arg: type) -> ret`) instead of wordy descriptions.
4.  **NO FLUFF:** Remove introductory text, "In this section...", "We will cover...", etc. Go straight to the facts.
5.  **AGGRESSIVE MERGING:** Combine related points. Instead of 3 bullet points, write one concise sentence.

PRIORITIZE:
1.  **Syntax & API Signatures:** The exact code structure is most important.
2.  **Critical Constraints:** What is allowed/forbidden.
3.  **Minimal Examples:** Only enough to show usage.

DE-PRIORITIZE:
1.  Tutorial-style explanations (how-to).
2.  Background/Theory (why it works).
3.  Marketing text (features, benefits).

OUTPUT FORMAT:
-   Use Markdown.
-   Use code blocks for syntax and examples.
-   Use concise bullet points for features/rules.

EXAMPLE INPUT:
```
[Source: A]
To declare a variable, use the 'has' keyword. It requires a type.
Example: has x: int = 5;

[Source: B]
Variables are defined with 'has'.
has name: str = "Jaseci";
You can also use access modifiers like :priv.
```

EXAMPLE OUTPUT:
```
**Variable Declaration**
Syntax: `has <name>: <type> = <value> by <access_modifier>;`
Modifiers: `:pub` (default), `:priv`, `:protect`.
Example:
```jac
has x: int = 5;
has name: str = "Jaseci" by :priv;
```

---
INPUT CONTENT:
{content}
---

Output ONLY the condensed reference documentation.
